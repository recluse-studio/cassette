# This file provides Codex UI metadata for the run-independent-build-review skill; dependencies: ../SKILL.md.
interface:
  display_name: "Run Independent Build Review"
  short_description: "Review one stage or an ordered stage batch"
  default_prompt: "Use $run-independent-build-review to challenge this frozen stage or declared stage batch with separate evidence and verdicts for every stage."
