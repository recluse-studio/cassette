# This file provides Codex UI metadata for the live-stage-review skill; dependencies: ../SKILL.md.
interface:
  display_name: "Live Stage Review"
  short_description: "Review sealed live evidence under its contract"
  default_prompt: "Use $live-stage-review to review this live attempt under its governing contract: replay every assertion, attack nearest false passes, apply only a proved bounded repair, seal the review, and return the baton without closing the stage."
